# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — deliberately broad. The old explicit list did not match
# a file named .env.staging or .env.prod, which is exactly how a service_role
# key reaches a public repo.
.env
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local Claude Code tool config — machine-specific, never commit
.claude/
